# Runtime deps for the venv flow (pyproject.toml is the canonical spec).
# Optional cross_encoder rerank extras (torch, sentence-transformers) are NOT
# here — install them only if you set RERANK_BACKEND=cross_encoder.
fastapi>=0.115
uvicorn>=0.30
psycopg[binary]>=3.2
psycopg-pool>=3.2
numpy>=2.0
ollama>=0.3
python-dotenv>=1.0
httpx>=0.27
pytest>=8.0
